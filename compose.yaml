services:
  ollama:
    image: 'ollama/ollama:latest'
    ports:
      - '11434:11434'
  postgres:
    image: 'postgres:17'
    environment:
      POSTGRES_DB: portfolio
      POSTGRES_USER: portfolio
      POSTGRES_PASSWORD: portfolio
    ports:
      - '5432:5432'
